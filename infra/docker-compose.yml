version: '3.3'

volumes:
  postgres_data:

services:

  db:
    image: postgres:13.0
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    env_file:
      - ./.env

  backend:
    build: ../block/
    depends_on:
      - db
    env_file:
      - ./.env